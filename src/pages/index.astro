---
// Detecta el idioma del navegador y redirige
const lang = Astro.request.headers.get('accept-language') || '';
const esIngles = lang.toLowerCase().startsWith('en');

return Astro.redirect(esIngles ? '/en' : '/es');
---